# Sa-Ra AI — Frontend Prototype

This is a frontend-only React + Vite prototype for the Sa‑Ra AI assistant UI/UX.

Commands:

```
npm install
npm run dev
```

What to try:
- Toggle the sidebar with the hamburger
- New Chat, search conversations
- Send messages (mocked assistant replies)
- Open Settings and Profile menus
